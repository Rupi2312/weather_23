

class WeatherModel{
  
  
  String city;
   String currentTemperature;
  String currentWeatherCondition;
  String currentImage;

  WeatherModel.name(this.city, this.currentTemperature,
      this.currentWeatherCondition, this.currentImage);
}